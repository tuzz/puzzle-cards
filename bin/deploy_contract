#!/bin/bash

# Deploys the PuzzleCard contract to the given network (see hardhat.config.js).

if [ "$#" -ne 1 ]; then
  echo "Usage: ./bin/deploy network"
fi

rm -f contracts/test_utils/TestUtils.sol && \
yarn hardhat run bin/deploy.js --network $1
